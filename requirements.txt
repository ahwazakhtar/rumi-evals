pandas>=2.0
numpy>=1.24
scipy>=1.10
pyyaml>=6.0
pydantic>=2.0
anthropic>=0.40          # LLM judge (steps 3a, 3b, 4a, 4b)
jiwer>=3.0               # WER/CER (step 2, once gold transcripts exist)
# google-cloud-bigquery>=3.11   # optional: uncomment for live warehouse reads (data.backend: bigquery)
